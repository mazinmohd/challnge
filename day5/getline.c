#include "main.h"


void get_command(void)
{
	/*char *line;
	size_t n;
	ssize_t chars;
*/
	printf("$");
	/*chars = getline(&line, &n, stdin);
	
	if (chars == -1)
	{
		write(1, "\n", 2);
		return (NULL);
	}*/

}
